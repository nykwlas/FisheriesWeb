For install:

npm install

---------------------------------------------------

For developing:

npm start   or

sudo PORT=80 serve -s build

---------------------------------------------------

For production:

npm run build 

sudo firebase deploy --project fisheries-2c0cb